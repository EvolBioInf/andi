\documentclass{minimal}

\usepackage{tikz}
\usetikzlibrary{plotmarks}

% Load the library
\usetikzlibrary{external}

% Activate it
\tikzexternalize

\usepackage{ucs}
\usepackage[utf8x]{inputenc}
%\usepackage{babel}
\usepackage{fontenc}
\usepackage{graphicx}


\usepackage{pgfplots}
\pgfplotsset{compat=1.9}
%\PrerenderUnicode{±}

\begin{document}

\pgfplotsset{every axis legend/.append style={
at={(0,1)},
anchor=north west}}

\begin{tikzpicture}
\begin{loglogaxis}[
	title=Accuracy,
	xlabel={Simulated Distance $K$},
	ylabel={Estimated Distance $d$},
]

\addplot+[only marks,
  error bars/.cd,
  y dir=both,
  y explicit,
] table[x=x,y=y,y error=error] {../data/spaced_tikz.acc100};
\addlegendentryexpanded{$d_{sw}$}

\addplot+[only marks,
  error bars/.cd,
  y dir=both,
  y explicit,
] table[x=x,y=y,y error=error] {../data/andi_tikz.acc100};
\addlegendentryexpanded{$d_{a}$}

\addplot+[only marks,
  error bars/.cd,
  y dir=both,
  y explicit,
] table[x=x,y=y,y error=error] {../data/cophylog_tikz.acc100};
\addlegendentryexpanded{$d_{co}$}

\addplot+[only marks,
  error bars/.cd,
  y dir=both,
  y explicit,
] table[x=x,y=y,y error=error] {../data/kr_tikz.acc100};
\addlegendentryexpanded{$d_{kr}$}

\addplot+[only marks,
  error bars/.cd,
  y dir=both,
  y explicit,
] table[x=x,y=y,y error=error] {../data/mugsy.acc100};
\addlegendentryexpanded{$d_{\mathit{mugsy}}$}

\addplot+[only marks,
  error bars/.cd,
  y dir=both,
  y explicit,
] table[x=x,y=y,y error=error] {../data/jellyfish_tikz.acc100};
\addlegendentryexpanded{$d_{\mathit{kmer}}$}


\addplot[green] coordinates {(0.0001,0.0001) (1,1)};
%\addlegendentryexpanded{ideal}

\end{loglogaxis}
\end{tikzpicture}

\end{document}
