\documentclass{minimal}

\usepackage{ucs}
\usepackage[utf8x]{inputenc}
%\usepackage{babel}
\usepackage{fontenc}
\usepackage{graphicx}

\usepackage{tikz}
\usetikzlibrary{plotmarks}

\usepackage{pgfplots}
\pgfplotsset{compat=1.9}
%\PrerenderUnicode{±}

% Load the library
\usetikzlibrary{external}

% Activate it
\tikzexternalize

\begin{document}

\pgfplotsset{every axis legend/.append style={
at={(0,1)},
anchor=north west}}

\begin{tikzpicture}
\begin{axis}[
	title=Accuracy,
	xlabel={Simulated Distance $K$},
	ylabel={Estimated Distance $d$},
]

\addplot+[only marks,
  error bars/.cd,
  y dir=both,
  y explicit,
] table[x=x,y=y,y error=error] {../data/spaced_lin.acc100};
\addlegendentryexpanded{$d_{sw}$}

\addplot+[only marks,
  error bars/.cd,
  y dir=both,
  y explicit,
] table[x=x,y=y,y error=error] {../data/andi_lin.acc100};
\addlegendentryexpanded{$d_{a}$}

\addplot+[only marks,
  error bars/.cd,
  y dir=both,
  y explicit,
] table[x=x,y=y,y error=error] {../data/cophylog_lin.acc100};
\addlegendentryexpanded{$d_{co}$}

\addplot+[only marks,
  error bars/.cd,
  y dir=both,
  y explicit,
] table[x=x,y=y,y error=error] {../data/kr_lin.acc100};
\addlegendentryexpanded{$d_{kr}$}

\addplot+[only marks,
  error bars/.cd,
  y dir=both,
  y explicit,
] table[x=x,y=y,y error=error] {../data/mugsy_lin.acc100};
\addlegendentryexpanded{$d_{\mathit{mugsy}}$}

\addplot[green] coordinates {(0.1,0.1) (1,1)};
\addlegendentryexpanded{ideal}

\end{axis}
\end{tikzpicture}

\end{document}
